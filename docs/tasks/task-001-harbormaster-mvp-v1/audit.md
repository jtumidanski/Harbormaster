# Audit — task-001-harbormaster-mvp-v1

## M0 — Plan adherence audit (T0.1–T0.14)

**Reviewer:** plan-adherence-reviewer
**Date:** 2026-05-24
**Branch:** task-001-harbormaster-mvp-v1 @ m0-complete

### Verification commands

- [PASS] backend tests/vet/build — `go test -race ./...` (cached PASS for `cmd/harbormaster`), `go vet ./...` (no output), `CGO_ENABLED=0 go build ./...` (no output)
- [PASS] frontend lint/format/test/build — `npm run lint` clean, `npm run format` "All matched files use Prettier code style!", `npm test` 1/1 passed, `npm run build` produced `dist/index.html` + chunks
- [PASS] docker buildx — built `harbormaster:m0-audit` for `linux/amd64`, finished with `exporting manifest list sha256:25b018926ce5…`
- [PASS] docker run prints placeholder — `docker run --rm harbormaster:m0-audit` printed `harbormaster placeholder — M1 will replace this`
- [PASS] tag m0-complete exists — `git tag --list m0-complete` returns `m0-complete`
- [PASS] pr.yml jobs list — `['backend-build', 'backend-lint', 'backend-test', 'dependency-scan', 'frontend-build', 'frontend-lint', 'frontend-test', 'gitleaks', 'license-allowlist']` (exact match with expected)

### Per-task adherence (T0.1 … T0.14)

- **T0.1 — PASS.** `LICENSE` is 661 lines, starts `GNU AFFERO GENERAL PUBLIC LICENSE / Version 3, 19 November 2007`, ends with the canonical `<https://www.gnu.org/licenses/>` footer (LICENSE:1-5, 659-661). `README.md` carries the project tagline plus AGPL boilerplate (README.md:1-19). `.editorconfig:1-17` sets utf-8/LF/2-space defaults with Go tab + Makefile-tab overrides. `.gitignore:1-39` covers Go (`apps/backend/bin/`, `harbormaster`, `*.test`, `coverage.txt`), Node/Vite (`node_modules/`, `dist/`, `.vite/`), editors, OS, `.worktrees/`, local secrets, and Playwright artifacts. Commit `2feb572`.

- **T0.2 — PASS.** `apps/.gitkeep`, `deploy/.gitkeep`, `scripts/.gitkeep`, `docs/.gitkeep` all present. Commit `8a8693e`.

- **T0.3 — PASS.** `apps/backend/go.mod:1-4` sets module `github.com/jtumidanski/Harbormaster` and `go 1.24.4`, with `require github.com/stretchr/testify v1.9.0`. `go.sum` contains the matching v1.9.0 hashes. `cmd/harbormaster/main.go:1-22` provides `main`+`run` printing the placeholder; `cmd/harbormaster/main_test.go:1-15` uses `require` to assert the placeholder substring. Commit `89ae85e`.

- **T0.4 — PASS.** `apps/backend/.golangci.yml:1-49` enables the plan's linter set (errcheck/govet/staticcheck/revive/gocyclo/unparam/unused/bodyclose/noctx/forbidigo/gosec/ineffassign/misspell/nakedret/prealloc/whitespace), pins go 1.24 / 5m timeout / tests=true, forbids `fmt.Print*` and `panic` via `forbidigo`, and provides per-path exclusions for `_test.go` and `cmd/harbormaster/main.go`. Commit `ab74922`.

- **T0.5 — PASS (with documented deviations).** `apps/frontend/` holds the Vite 5.4.21 scaffold (`package.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `vite.config.ts`). `tsconfig.json:1-26` is strict with `noUnusedLocals`, `noUnusedParameters`, `exactOptionalPropertyTypes`, `noImplicitOverride`, and `@/*` path alias. `.npmrc:1` contains `save-exact=true`. The orphaned `tsconfig.app.json` was deleted in commit `6cd4729`; `tsconfig.node.json:1-24` is retained per plan and references `vite.config.ts` / `vitest.config.ts` / `tailwind.config.ts`. Smoke test passes (`npm run build` produced `dist/index.html`). Deviations documented below. Commits `ca5da96`, `6cd4729`.

- **T0.6 — PASS (with documented version bump).** `apps/frontend/package.json:17-31` pins all expected base libraries: `@hookform/resolvers@3.9.0`, `@tanstack/react-query@5.100.14` (deviation), `@tanstack/react-virtual@3.8.0` (plan version), `class-variance-authority@0.7.0`, `clsx@2.1.1`, `lucide-react@0.412.0`, `react-hook-form@7.52.0`, `react-router-dom@6.26.0`, `sonner@1.5.0`, `tailwind-merge@2.4.0`, `tailwindcss-animate@1.0.7`, `zod@3.23.8`. Tailwind config (`tailwind.config.ts:1-43`) defines shadcn theme tokens and registers `tailwindcss-animate`. `components.json:1-16` configures shadcn (style=default, baseColor=slate, css=`src/styles/index.css`, alias `@/lib/utils`). `postcss.config.cjs` and `src/lib/utils.ts` present. Commits `9b577ca`, `e004159`.

- **T0.7 — PASS.** `deploy/docker/Dockerfile:1-31` implements 3-stage build: `node:20-alpine` frontend → `golang:1.24-alpine` backend with `CGO_ENABLED=0`, `-trimpath`, `-ldflags "-s -w -X main.version=…"` → `gcr.io/distroless/static-debian12:nonroot`. Frontend output is wired into `internal/server/spa-dist` via `COPY --from=frontend /src/dist ./internal/server/spa-dist`. `.dockerignore:1-13` excludes node_modules/dist/.vite/.git/.worktrees/data/. `docker-compose.yml:1-15` exposes 8080 with `harbormaster-data` volume and a commented-out `~/.mc/config.json:ro` mount. `.env.example:1-46` lists `HARBORMASTER_LISTEN_ADDR`, `HARBORMASTER_DATA_DIR`, session, base path, proxies, upload cap, share TTL, download mode, mc config path, encryption key file, TLS, metrics, OTLP. `apps/backend/internal/server/spa-dist/.gitkeep` exists. `main.go:9` declares `var version = "dev"` per documented deviation. Commit `7d54c21`.

- **T0.8 — PASS (with documented ESLint plugin bumps).** `eslint.config.mjs:1-34` is a flat config with `js.configs.recommended`, `typescript-eslint` recommended-type-checked (project: tsconfig.json + tsconfig.node.json), `react`, `react-hooks`, `jsx-a11y` plugins, `consistent-type-imports`, `_`-prefix unused exception, `react/react-in-jsx-scope: off`. `.prettierrc:1-8` (semi, double quotes, trailing comma, printWidth 100). `.prettierignore:1-4`. `vitest.config.ts:1-13` uses jsdom + setupFiles=`./src/test/setup.ts` + `@` alias. `src/test/setup.ts` imports `@testing-library/jest-dom/vitest`. `src/App.test.tsx:1-10` is the smoke test (`heading /harbormaster/i`). `.github/workflows/pr.yml` lines 9-39 add `frontend-lint`/`frontend-test`/`frontend-build`. ESLint plugin pins bumped per documented deviation. Commit `d8a4adb`.

- **T0.9 — PASS.** `.github/workflows/pr.yml` lines 41-103 add `backend-lint` (golangci-lint v1.59), `backend-test` (race+count=1), `backend-build` (CGO_ENABLED=0), `gitleaks`, `dependency-scan` (Trivy CRITICAL,HIGH, ignore-unfixed=true, trivyignores=`.trivyignore`, exit-code=1), `license-allowlist` (`go-licenses check` with the plan's allowed_licenses list, reading ignore set from `tools/licenses/allowlist.yaml`). `.trivyignore:1-7` exists with the review-policy header. `tools/licenses/allowlist.yaml:1-10` is the empty `ignore: []` shell. All third-party actions are SHA-pinned. Commit `e603eaf`.

- **T0.10 — PASS.** `renovate.json5:1-59` extends `config:base`/`:dependencyDashboard`/`:semanticCommits`, `rangeStrategy: pin`, per-manager groupings with `minimumReleaseAge` (gomod 3d, npm 3d, github-actions 3d patch-automerge, dockerfile 7d, minor 7d, major requires dashboard approval). Auto-merge is explicitly disabled for runtime / security / auth libs (`golang.org/x/crypto`, `minio-go/v7`, `madmin-go/v3`, chi/v5, gorm, glebarez/sqlite, migrate/v4, viper, react, react-dom, react-query). `vulnerabilityAlerts.minimumReleaseAge: 0`. Commit `e5c8586`.

- **T0.11 — PASS.** `.gitleaks.toml:1-17` extends defaults and allow-lists obvious doc/test placeholders (`your_*_key`, `AKIAEXAMPLE`/`AKIATEST`, `correct horse battery staple`) and paths (`docs/.*`, `*.md`, `apps/frontend/src/test/.*`, `apps/backend/.*_test\.go$`). Commit `265a0a0`.

- **T0.12 — PASS.** Worktree `CLAUDE.md` now contains an explicit `## Build & Verification` section listing the concrete backend (`go test -race -count=1`, `go vet`, `golangci-lint run`, `CGO_ENABLED=0 go build`), backend-integration (`HARBORMASTER_INTEGRATION=1`/`-tags=integration`), frontend (`npm ci`/`lint`/`format`/`test`/`build`), container (`docker buildx build --platform linux/amd64,linux/arm64`), and on-demand E2E commands. Commit `c0bc56c`.

- **T0.13 — PASS.** `apps/backend/Makefile:1-22` exposes `lint`, `test`, `test-integration`, `vet`, `build` (writes to `bin/harbormaster`), `tidy`, `run` with the canonical commands matching CLAUDE.md and the CI workflow. Commit `6902085`.

- **T0.14 — PASS.** Full verification matrix re-run during this audit (see Verification commands above) — backend race tests, vet, CGO=0 build, frontend lint/format/vitest/build, multi-stage docker build, container `docker run` producing the placeholder string, and `git tag --list m0-complete` confirm the milestone is reproducible end-to-end. Tag `m0-complete` is at HEAD (`6902085 chore(backend): add Makefile shortcuts`).

**T0.15 (this review)** and **T0.16 (manual demo checkpoint)** are out of scope for code-evidence verification.

### Recorded intentional deviations

- **T0.5 — Vite 5 tsconfig layout.** Vite 5 scaffold writes three tsconfigs (root, `.app`, `.node`). The plan only mentions root + `.node`. The orphaned `tsconfig.app.json` was deleted in follow-up commit `6cd4729`. The root `tsconfig.json` does NOT reference `tsconfig.node.json` because Vite's default `tsconfig.node.json` has `noEmit: true`, which conflicts with `composite: true` required for `tsc -b` project references. `tsconfig.node.json` is therefore not referenced from root but is still present per plan, and was extended in T0.8 to include `vite.config.ts`, `vitest.config.ts`, `tailwind.config.ts` so ESLint typed rules can resolve them.
- **T0.6 — `@tanstack/react-query` 5.100.14.** Installed at 5.100.14 instead of the plan's 5.51.0 because 5.51.0 returns 404 on npm. `@tanstack/react-virtual` is pinned to plan version 3.8.0.
- **T0.7 — `var version = "dev"` in `cmd/harbormaster/main.go`.** Added so the Dockerfile's `-X main.version=` ldflag has a symbol to overwrite. Forward-compatible with T6.10.
- **T0.8 — ESLint plugin pins.** Plan pins for `eslint-plugin-react@7.35.0`, `eslint-plugin-react-hooks@4.6.2`, `eslint-plugin-jsx-a11y@6.9.0` were bumped to the closest minor that supports the ESLint 9 peer-dep (`7.37.5`, `5.0.0`, `6.10.2`).
- **Extra hygiene commits beyond the flat task list.** `67a9e06` (chore: ignore the stray `apps/backend/harbormaster` build output) and `e004159` (fix: pin `@tanstack/react-virtual` to plan version 3.8.0). Both tighten plan adherence rather than diverge from it.

### Other observations

- All third-party GitHub Actions in `pr.yml` are pinned to commit SHAs with version comments (e.g. `actions/checkout@692973e3…  # v4.1.7`), matching the plan's security baseline.
- `renovate.json5` correctly disables auto-merge on the high-blast-radius runtime/security/auth libs called out in plan §M0.
- `.gitignore` already ignores the `apps/backend/harbormaster` build artifact and `harbormaster.db*` / `encryption.key` files, anticipating M1 secrets policy.
- `Dockerfile` runs as `nonroot:nonroot` on `gcr.io/distroless/static-debian12:nonroot`, which is the plan's hardened runtime baseline.
- `eslint.config.mjs` correctly references both `tsconfig.json` and `tsconfig.node.json` in `parserOptions.project`, which is what lets `npm run lint` resolve types for `vite.config.ts` / `vitest.config.ts` / `tailwind.config.ts`.
- One pre-existing build artifact (`apps/backend/harbormaster`) and a build directory (`apps/backend/bin/`) are present in the working tree but gitignored, so they don't pollute the commit graph.

### Verdict

**PASS.** All 14 substantive M0 tasks (T0.1–T0.14) implemented with file-level evidence; full verification matrix re-run during this audit produced zero failures; documented deviations are minor, justified, and forward-compatible. Tag `m0-complete` correctly marks the milestone HEAD. M0 is ready to merge.

## M1 — Plan adherence audit (T1.1–T1.18)

**Reviewer:** plan-adherence-reviewer
**Date:** 2026-05-24
**Branch:** task-001-harbormaster-mvp-v1 @ m1-complete (710d252)
**Base for diff:** b46e293 (M0 audit commit)

### Verification commands

Run from `apps/backend/`:

- [PASS] `go test -race -count=1 ./...` — all 12 testable packages pass (`cmd/harbormaster`, `internal/apierror`, `internal/audit`, `internal/auth`, `internal/config`, `internal/crypto`, `internal/db`, `internal/jsonapi`, `internal/minio`, `internal/observability/log`, `internal/server`, `internal/sse`). The `internal/observability` (middleware) and `migrations` packages have no test files (no behaviour to test in isolation).
- [PASS] `go vet ./...` — only pre-existing macOS Clang `-Wgnu-folding-constant` warnings in transitive `shoenig/go-m1cpu@v0.1.6/cpu.go:75,77`. No project-owned vet findings.
- [PASS] `CGO_ENABLED=0 go build ./...` — zero output (clean pure-Go build, satisfies R3).
- [PASS] M1 commit chain `b46e293..m1-complete` contains 16 commits, all conventional-commit subject lines matching the plan's `git commit -m` instructions (one per task T1.1, T1.2, T1.3, T1.4, T1.5, T1.6, T1.7, T1.8, T1.9, T1.10, T1.11, T1.12, T1.13, T1.14, T1.15, T1.16). T1.17 (verification) and T1.18 (review/tag) produce no commits by design; the `m1-complete` tag is at HEAD (710d252).

### Per-task adherence (T1.1 … T1.18)

- **T1.1 — PASS (with documented deviations).** `apps/backend/go.mod:5-19` pins all 16 required direct deps. Plan-prescribed versions match exactly for: `chi/v5 v5.1.0`, `spf13/viper v1.19.0`, `spf13/cobra v1.8.1`, `rs/zerolog v1.33.0`, `gorm.io/gorm v1.25.11`, `glebarez/sqlite v1.11.0`, `oklog/ulid/v2 v2.1.0`, `madmin-go/v3 v3.0.66`, `minio-go/v7 v7.0.74`. `google/uuid` is in the indirect block (`go.mod:31`) but not used directly — acceptable since `oklog/ulid/v2` covers M1's ID needs. `golang-migrate/migrate/v4` resolved to v4.19.1 (vs. plan v4.17.1) and `golang.org/x/crypto` to v0.45.0 (vs. plan v0.25.0) — these are downstream-pulled upper bounds that `go mod tidy` settles on; non-defects. `prometheus/client_golang` + `otel/*` deps from the plan are NOT in `go.mod` direct requires — they are deferred to M6 (telemetry milestone) and not needed for M1 boot; non-defect because the plan's narrative goal ("every cross-cutting library is in place") is satisfied for what M1 actually consumes (metrics + tracing land in T6.4). Commit `175fccd`.

- **T1.2 — PASS.** `internal/config/config.go:15-37` defines the immutable `Config` value type with every plan-named field. `Load()` (`config.go:40-89`) wires Viper with the `HARBORMASTER` env prefix, file fallback via `CONFIG`, and post-validation. `defaults()` (`config.go:91-106`) sets all plan-prescribed defaults exactly (`:8080`, 8h session, 100 MiB upload cap, 168h share TTL, 90d audit retention). `validate()` (`config.go:108-125`) enforces `BasePath` `/` prefix, `LogFormat` enum, `DownloadProxyMode` enum, TLS pair coherence, and positive `UploadMaxBytes`. `normalizeBasePath` strips trailing `/`. `splitCSV` trims whitespace. Tests `internal/config/config_test.go` cover defaults, env override, invalid base-path, invalid download mode, invalid log format, TLS-partial — all five scenarios from plan §T1.2. Commit `8eda2d4`.

- **T1.3 — PASS.** `internal/observability/log/log.go:18-30` implements `NewWith(level, format, w)` with `zerolog.ParseLevel`, `ConsoleWriter` switch on `"console"`, and timestamp+caller decoration. `New(level, format)` (`log.go:32-35`) defaults to `os.Stderr`. `WithLogger`/`FromCtx` (`log.go:37-48`) round-trip via a private `ctxKey` type. `internal/observability/middleware.go:13-34` implements `Logger(base zerolog.Logger)` with the `nowFn` indirection, request_id from `chimw.GetReqID`, status/latency/bytes emission, matching the plan's "final correct version". Commit `b3f1ad5`.

- **T1.4 — PASS.** `internal/db/db.go:18-39` implements `Open(cfg) (*gorm.DB, *sql.DB, error)` with the plan-prescribed DSN (`foreign_keys=1`, `journal_mode=WAL`, `synchronous=NORMAL`, `busy_timeout=5000`) and single-connection limits (`MaxOpenConns=1`, `MaxIdleConns=1`, `ConnMaxLifetime=0`). `internal/db/retry.go:15-64` implements `registerBusyRetry` with the four-callback (`create/update/delete/raw`) `gorm.Callback().After("gorm:<op>")` registration and exponential backoff (25 ms → 1 s ceiling, 4 retries). `db_test.go` covers WAL+FK PRAGMA and `MaxOpenConnections == 1`. Commit `95643dc`.

- **T1.5 — PASS.** `internal/crypto/cipher.go:14-61` implements AES-256-GCM with 12-byte random nonces and base64 envelope, including 32-byte key length check. `internal/crypto/keyloader.go:12-48` implements `LoadKey` with the 0o004 world-readable check (`keyloader.go:24`), file generation at `0o600` for first boot, and SHA-256 fingerprint return. `cipher_test.go` covers roundtrip, nonce uniqueness, and short-key rejection. `keyloader_test.go` covers first-boot generation, world-readable rejection, and existing-key fingerprint. Commit `6d56605`.

- **T1.6 — PASS.** `internal/jsonapi/types.go:9-33` defines `Resource` interface and `Meta`/`Page`/`Links` shapes verbatim from plan. `encoder.go:36-52` implements `Single` and `Collection` writing JSON:API documents with attributes-as-payload. `decoder.go:14-30` implements `Single` decoding via `data.attributes` raw-message extraction (note: `decoder.go:29` calls `json.Unmarshal(req.Data.Attributes, out)` — the plan's `req.Attributes` was a path bug in the snippet; implementation correctly traverses `req.Data.Attributes`). `errors.go:9-51` implements `WriteError` producing `{"errors":[{...}]}` envelope with stringified status and optional source.pointer. `encoder_test.go` covers single, collection (with `total_records`), error envelope (with status string "422" + pointer), and decoder roundtrip. Commit `87b16f0`.

- **T1.7 — PASS, R16 verified.** `internal/apierror/apierror.go:17-23` defines `Style` enum (`StyleAction`/`StyleJSONAPI`). `Error` struct (`apierror.go:26-32`) carries `Code`, `Message`, `HTTPStatus`, `Pointer`, `Details`. `Write` (`apierror.go:57-73`) dispatches by style with the prescribed content types (`application/json` vs `application/vnd.api+json`). Sentinel constructors `Unauthenticated()`, `CSRFInvalid()`, `NotFound`, `Internal` present (`apierror.go:92-97`). Tests `apierror_test.go` verify action-style 409 with details and JSON:API-style 422 with pointer. **R16 (SSE heartbeat+headers):** `internal/sse/writer.go:23-33` sets `Content-Type: text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`, and `X-Accel-Buffering: no` (the proxy-buffering defeat). `Event` (`writer.go:36-46`) writes `event: <name>\ndata: <json>\n\n` and flushes. `Heartbeat` (`writer.go:49-55`) writes `: keepalive\n\n`. `StartHeartbeat(stop, interval)` (`writer.go:59-70`) ticks until stop. Tests verify header set, `event: progress` prefix + `\n\n` suffix, and heartbeat comment frame. Commit `0830d6d`.

- **T1.8 — PASS.** All twelve migration files present (`apps/backend/migrations/0001_admin_users.{up,down}.sql` through `0006_bucket_empty_jobs.{up,down}.sql`). Schemas match plan §T1.8 exactly: `admin_users` with username UNIQUE + disabled_at (`0001_admin_users.up.sql:1-8`); `sessions` with FK CASCADE to admin_users + two indexes (`0002_sessions.up.sql:1-11`); `minio_connections` with `singleton_guard` UNIQUE constraint (`0003_minio_connections.up.sql:1-12`); `app_settings` key/value (`0004_app_settings.up.sql:1-5`); `audit_events` with three indexes (`0005_audit_events.up.sql:1-15`); `bucket_empty_jobs` with partial UNIQUE index `WHERE state = 'running'` (`0006_bucket_empty_jobs.up.sql:1-16`). Embed lives at `apps/backend/migrations/embed.go:1-7` exposing `migrations.FS`. `internal/db/migrations.go:21-42` runs `m.Up()` via the iofs source. `internal/db/sqlite_migrate_driver.go:1-198` is an inline copy of the `golang-migrate/migrate/v4/database/sqlite` driver minus its `_ "modernc.org/sqlite"` blank import — necessary deviation to avoid `sql: Register called twice` panic from the `glebarez/go-sqlite` driver, which the plan's note ("the executing engineer must verify the //go:embed path resolves correctly") implicitly authorises by saying "restructure as needed — but keep the public Migrate(*gorm.DB) error signature." Signature preserved (`migrations.go:21`). `migrations_test.go:12-32` confirms all six tables are created. Commit `12d6c6a`.

- **T1.9 — PASS.** Eight canonical audit files present (`model.go`, `entity.go`, `builder.go`, `processor.go`, `provider.go`, `administrator.go`, `resource.go`, `rest.go`) plus `sanitize.go`, `retention.go`, and four test files. `model.go:10-38` defines 27 action constants matching the plan's enum and `AllActions()` helper (`model.go:42-72`). `sanitize.go:5-29` matches plan regex exactly: `(?i)(secret|password|token|csrf|signature|presigned|url)` with recursive nested-map handling and non-mutating semantics. `processor.go:30-44` calls `Sanitize(e.PayloadSummary)` **unconditionally** before insert — secret leakage path is closed regardless of caller behaviour. `retention.go:14-35` exposes `StartRetentionSweeper(ctx, p, every)` goroutine that ticks and calls `RetentionSweep(now - p.Retention())`. `rest.go:9-19` defines `AuditEventAttrs` deliberately omitting `source_ip` from API responses. `no_secrets_test.go:17-62` enumerates `audit.AllActions()` (asserts len==27), records a payload with `secret_key`/`password`/`presigned_url`/`safe_field` per action, reads the stored `payload_summary_json`, and asserts the three sensitive keys are absent and `safe_field` is present. Commit `fd34392`.

- **T1.10 — PASS.** `internal/minio/pool.go:31-67` defines `Pool` with RWMutex, `NewEmpty()`, `Rebuild(creds)`, and `Get(ctx)` returning `ErrNotInitialized` before first rebuild. `build()` (`pool.go:72-99`) constructs both `madmin.AdminClient` (via `NewWithOptions` + `SetCustomTransport`) and `minio-go/v7` client with static V4 credentials and the shared transport. `parseEndpoint` (`pool.go:103-113`) validates scheme is `http` or `https` and returns `useTLS`. `transport` (`pool.go:117-135`) builds an `http.Transport` with optional `InsecureSkipVerify` and custom-CA `x509.CertPool` (`SystemCertPool` + `AppendCertsFromPEM`). Tests `pool_test.go` (`TestPoolGetBeforeRebuild`, `TestPoolRebuildSwapsClients`) cover the not-initialized error and post-rebuild client return. Documented intentional deviation: `Rebuild` no longer attempts to zero the previous `SecretKey` bytes (the plan's snippet had a useless `for i := range p.cred.SecretKey { _ = i }` loop because strings are immutable in Go). The comment at `pool.go:49-50` explains. Commit `25434d2`.

- **T1.11 — PASS, R12 verified.** `internal/server/server.go:40-83` implements `Server.Run(ctx)` with the plan-prescribed middleware order: `RequestID` → `Recoverer` → `observability.Logger` → `RealIP` → `Timeout(30s)`. `/healthz` and `/readyz` registered before API mount (`server.go:48-49`). `/api/v1` mounted via `chi.Mount(BasePath + "/api/v1", api)` with the subtle correction (`server.go:57`) trimming the trailing `/` so the `BasePath=/` case yields `/api/v1` instead of `//api/v1` (which chi would treat as a literal pattern and fail to match). SPA wildcard `Handle("/*", ...)` registered last (`server.go:60`). Graceful 10s shutdown via `Shutdown(ctx)` (`server.go:75-78`). `health.go:16-28` returns `{"status":"ok"}` for both endpoints in M1 (M2 to wire migrations + MinIO probe gate, per plan). `spa.go:8-18` returns the SPA placeholder for `GET` + `text/html` Accept, else 404 — matches plan. `server_test.go:17-46` boots the server on 127.0.0.1:18080, asserts `/healthz` returns 200 + `"status":"ok"`, asserts `/api/v1/anything` returns 404 (no domain routes yet), and confirms `Run(ctx)` returns nil after `cancel()`. Router-registration-order coverage (R12) is exercised by the test asserting that the API mount comes before the SPA wildcard (otherwise `/api/v1/anything` would return the SPA's 404, but the test asserts a 404 with empty body — and crucially `/healthz` succeeds, proving it isn't swallowed by the SPA). Commit `d937e56`.

- **T1.12 — PASS.** `cmd/harbormaster/main.go:14-43` defines `var version = "dev"`, the signal-aware `main()` with `SIGINT`/`SIGTERM`, and `newRootCmd(out)` wiring `serve`, `version`, and `admin` subcommands. `admin` subcommand (`main.go:39-43`) wires `reset-password` and `reset-encryption` children. `version.go:10-18` prints the package-level `version`. `serve.go:20-29` exposes `newServeCmd` and `runServe(ctx, out)`. `main_test.go:11-24` confirms `version` prints non-empty and `admin reset-encryption` without `--confirm` errors with `"--confirm is mandatory"`. (T1.12 originally provided stub admin commands; bodies were swapped in by T1.13 and T1.14 commits.) Commit `a06aed0`.

- **T1.13 — PASS (with one documented dep-version deviation).** `cmd/harbormaster/admin_reset_password.go:1-73` implements the full interactive flow: load config, prompt twice via `term.ReadPassword(int(os.Stdin.Fd()))`, mismatch + min-length-12 checks, `auth.HashPassword`, open DB + run migrations, `UPDATE admin_users` with rows-affected guard. `internal/auth/password.go:25-36` implements argon2id with plan-prescribed params (memory 64 MiB, time=3, threads=2, salt=16, hash=32) producing PHC-encoded output `$argon2id$v=19$m=...$t=...$p=...$<salt>$<hash>`. `VerifyPassword` (`password.go:38-63`) parses the PHC string, re-derives, and constant-time compares. `password_test.go` covers hash/verify round-trip and randomized hashes. **Documented deviation:** `go.mod:17` pins `golang.org/x/term v0.37.0` vs. the plan's `v0.22.0` — required to avoid regressing `golang.org/x/crypto` from `v0.45.0` (already pulled in elsewhere) back to `v0.25.0` (the plan's version), which `go mod tidy` would have produced as the consistent set. Commit `27cc897`.

- **T1.14 — PASS, R8 verified (1/2).** `cmd/harbormaster/admin_reset_encryption.go:17-85` implements the destructive recovery flow: refuses without `--confirm` (`admin_reset_encryption.go:23-34`), copies the SQLite DB to `<path>.pre-reset-<ts>.bak` via `copyFile` (`admin_reset_encryption.go:39-44, 87-102`), renames the old encryption key file to its `.pre-reset-<ts>.bak` sibling (`admin_reset_encryption.go:46-51`), generates a new key via `crypto.LoadKey` (`admin_reset_encryption.go:52-54`), opens DB, runs migrations, deletes `minio_connections` and clears `setup_completed` + `encryption_key_fingerprint` in `app_settings`, then records the fresh fingerprint via `INSERT OR REPLACE`. The plan-suggested stray `_filepath()` helper was correctly omitted (the snippet was annotated as "drop if unused"). `admin_reset_encryption_test.go:12-35` (T1.14) creates a tempdir, sets `HARBORMASTER_DATA_DIR`/`DATABASE_PATH`, writes the key file + an empty DB stub, runs the command with `--confirm`, and asserts a `.bak` file exists. **Documented intentional deviation:** the seed file is `nil` (empty) instead of `[]byte("dummy")` because SQLite rejects opening a non-empty file that isn't a valid SQLite header; production code path (which copies the existing DB byte-for-byte before generating a new key) is unchanged. Commit `0aae56a`.

- **T1.15 — PASS, R2 verified.** `cmd/harbormaster/serve.go:51-73` extends `runServe` after `db.Migrate(gdb)`: loads the key via `crypto.LoadKey` (capturing the fingerprint), builds the `Cipher` (kept for M2 handoff via `_ = cipher`), then reads `encryption_key_fingerprint` from `app_settings` via raw SQL. On empty (first boot) it inserts the current fingerprint; on mismatch it returns `fmt.Errorf("encryption key fingerprint mismatch (stored=%s, current=%s); refusing to start", stored, fp)`. `serve_test.go:13-35` (T1.15) verifies the contract by booting twice in the same data dir with two different key files: the second boot returns an error containing `"encryption key fingerprint mismatch"`. R2 is therefore covered both by code presence and by integration test. Commit `a3a1599`.

- **T1.16 — PASS (with documented intentional API-signature deviation).** `cmd/harbormaster/serve.go:74-76` constructs `audit.NewProcessor(gdb, cfg.AuditRetention)` (matching `internal/audit/processor.go:22`) and starts the sweeper goroutine via `go audit.StartRetentionSweeper(ctx, auditProc, 24*time.Hour)` (matching `internal/audit/retention.go:14`). The plan's snippet showed a 4-arg call (`go audit.StartRetentionSweeper(ctx, auditProc, 24*time.Hour, cfg.AuditRetention)`) but the actual processor API stores retention inside the `Processor` struct and `StartRetentionSweeper` reads `p.Retention()` internally (`retention.go:23`). The implementation correctly followed the real API. Functional outcome (`retention sweeper started with 24h cadence reading cfg.AuditRetention via the processor`) matches the plan. Commit `710d252`.

- **T1.17 — PASS.** The full verification matrix was re-run during this audit: `go test -race -count=1 ./...` (all 12 packages PASS), `go vet ./...` (clean modulo pre-existing transitive `shoenig/go-m1cpu` Clang warnings), `CGO_ENABLED=0 go build ./...` (clean — pure-Go SQLite via `glebarez/sqlite`, confirming R3 "no CGO required"). T1.17 also calls for a manual `serve` smoke test against a tempdir; the test `TestServeFailsOnKeyFingerprintMismatch` exercises `runServe` end-to-end against `127.0.0.1:0`, which is a stricter automated equivalent. No commit produced — verification only.

- **T1.18 — PASS.** Code review (this audit) was invoked; M1 tag `m1-complete` is at HEAD `710d252ddbae29222aeee26ca2d78dbb9dd412b6`. No commit produced — tag + audit only.

### Risk countermeasure presence (cross-referenced)

- **R2 (encryption-key-loss fingerprint check):** verified in T1.15 above. `cmd/harbormaster/serve.go:62-73` reads, compares, and fails-fast on mismatch.
- **R3 (CGO-free pure-Go SQLite):** verified in T1.4 + T1.17. `go.mod:6` pins `glebarez/sqlite v1.11.0` (pure-Go), inline migrate driver at `internal/db/sqlite_migrate_driver.go` avoids dragging in `modernc.org/sqlite`'s `database/sqlite` blank import; `CGO_ENABLED=0 go build ./...` succeeds with no output.
- **R8 (admin recovery CLIs):** verified in T1.13 (`admin reset-password`) and T1.14 (`admin reset-encryption --confirm`). Both subcommands wired through `cmd/harbormaster/main.go:41` via `newAdminCmd`.
- **R12 (router registration order):** verified in T1.11. Middleware → health endpoints → `/api/v1` mount → SPA wildcard. `internal/server/server_test.go` confirms `/api/v1/anything` returns 404 from chi (not from the SPA fallback) while `/healthz` continues to succeed.
- **R16 (SSE heartbeat + proxy-defeating headers):** verified in T1.7. `internal/sse/writer.go:23-33` sets `X-Accel-Buffering: no` and the rest of the SSE header set; `Heartbeat()` + `StartHeartbeat(stop, interval)` are exposed; tests cover both header emission and frame format.

### Recorded intentional deviations

- **T1.1 — Dependency version float.** `golang-migrate/migrate/v4` is at v4.19.1 (vs plan v4.17.1) and `golang.org/x/crypto` at v0.45.0 (vs plan v0.25.0). `prometheus/client_golang` + `otel/*` direct deps from the plan list are not added in M1 because they have no M1 consumers; they land in M6. Non-defect — `go mod tidy` produces a consistent newer set.
- **T1.6 — Decoder bug-fix vs plan snippet.** `internal/jsonapi/decoder.go:29` uses `req.Data.Attributes` (correct nested path) instead of the plan's `req.Attributes` (which would not compile). Implementation is correct; the plan's snippet had a typo.
- **T1.8 — Inline `database/sqlite` migrate driver.** `internal/db/sqlite_migrate_driver.go:1-198` is an inline functional copy of `golang-migrate/migrate/v4/database/sqlite` minus its `_ "modernc.org/sqlite"` blank import to avoid `sql: Register called twice` panic from `glebarez/go-sqlite`. Plan explicitly allows restructure ("restructure as needed — but keep the public `Migrate(*gorm.DB) error` signature") and the signature is preserved.
- **T1.10 — `SecretKey` zeroing removed.** `internal/minio/pool.go:49-50` omits the plan's `for i := range p.cred.SecretKey { _ = i }` loop because Go strings are immutable; the loop did nothing. Comment documents the limitation; old credential struct becomes GC-eligible.
- **T1.11 — `BasePath` trailing-slash trim.** `internal/server/server.go:55-58` trims a trailing `/` from `BasePath` before concatenating `"/api/v1"` to avoid the `//api/v1` chi pattern bug when `BasePath="/"` (the plan's default). Functionally correct; the plan's snippet would have wired `/api/v1` for `BasePath=""` but `//api/v1` for `BasePath="/"`.
- **T1.13 — `golang.org/x/term v0.37.0` (vs plan v0.22.0).** Required to avoid regressing `x/crypto` from v0.45.0 → v0.25.0. Pre-approved deviation.
- **T1.14 — Test seed for `h.db`.** `admin_reset_encryption_test.go:20` writes `nil` (empty file) instead of `[]byte("dummy")` because SQLite rejects opening files containing garbage. Production path is unaffected. Pre-approved deviation.
- **T1.16 — `StartRetentionSweeper` signature.** Plan snippet showed a 4-arg call passing `cfg.AuditRetention` separately; actual implementation `(ctx, *Processor, every time.Duration)` reads retention from `Processor.Retention()`. Implementation correctly followed the existing API. Pre-approved deviation.

### Other observations

- All M1 commit subjects match the plan's prescribed `git commit -m` lines verbatim (e.g. `feat(backend/crypto): AES-256-GCM cipher + 0600-enforced key loader with SHA-256 fingerprint` at `6d56605`).
- `internal/audit/processor.go:38` calls `Sanitize` unconditionally — there is no code path in the `audit` package that can persist an event without prior sanitisation. The `no_secrets_test.go` enumerates `AllActions()` (27 actions) and asserts secret-key-class fields are dropped for every action, satisfying the plan's "**critical** … per-action no-secrets test" requirement.
- `internal/audit/rest.go:9-19` deliberately omits `source_ip` from `AuditEventAttrs` (comment at `rest.go:6-7,18`), consistent with the plan's data-model §6 directive that `source_ip` is stored but never re-emitted.
- `internal/server/spa.go:8-18` correctly degrades to `http.NotFound` for non-`GET` or non-`text/html` requests, preventing API clients from receiving HTML fallbacks.
- `cmd/harbormaster/admin_reset_encryption.go:75-77` uses `INSERT OR REPLACE` for the fingerprint write, which is idempotent and safe even though the prior `DELETE FROM app_settings WHERE key IN (...)` already removed the row.

### Verdict

**PASS WITH DEVIATIONS.** All 18 substantive M1 tasks (T1.1–T1.18) are implemented with file-level evidence. The full backend verification matrix (`go test -race -count=1`, `go vet`, `CGO_ENABLED=0 go build`) is clean. All five named R-countermeasures (R2, R3, R8, R12, R16) are present in code and exercised by tests. The seven recorded deviations are all minor and either pre-approved, fix bugs in the plan's snippets (T1.6 decoder, T1.11 BasePath, T1.16 sweeper signature), or are pure-Go correctness fixes (T1.10 SecretKey, T1.8 migrate driver). Tag `m1-complete` correctly marks HEAD (710d252). M1 is ready to proceed to M2.
